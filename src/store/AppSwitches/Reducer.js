import {} from './Action';

const initialState = {};

export const myMainAppReducer = (state = initialState, action) => {
    switch(action.type) {
        default: {
            return state
        }
    }
};
